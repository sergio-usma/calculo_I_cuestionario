# 📝 CUESTIONARIO DE CÁLCULO I
## Análisis de funciones y relaciones en $\mathbb{R} \times \mathbb{R}$

---

### Ejercicio 1

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = 2x - 3
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(1, -1)$ pertenece a la gráfica.
- La expresión representa una función lineal.
- El dominio es $\mathbb{R}$.
- La función es estrictamente creciente.
- El rango es $[-3, \infty)$.
- La gráfica corta al eje $y$ en $y = -3$.
- La función es biyectiva.
- La pendiente es positiva.

#### Sección 5 — Nivel estructural avanzado
- Compare esta función con $y = 2x$. ¿Qué cambio geométrico ocurre?
- Si se modifica la expresión a $y = |2x - 3|$, ¿sigue siendo una función? ¿Qué cambia en el rango?

---

### Ejercicio 2

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = x^2 - 4x + 3
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente (use vértice y discriminante).
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(2, -1)$ pertenece a la gráfica.
- La función es cuadrática.
- El discriminante es positivo.
- El vértice es $(2, -1)$.
- El dominio es $\mathbb{R}$.
- El rango es $[-1, \infty)$.
- La función es par.
- La gráfica corta al eje $x$ en $x=1$ y $x=3$.

#### Sección 5 — Nivel estructural avanzado
- Compare esta función con $y = x^2 - 4x + 4$. ¿En qué se diferencian sus gráficas?
- Si se define $F = \{(x,y) \in \mathbb{R} \times \mathbb{R} : y = x^2 - 4x + 3, \ x \in [2,5]\}$, ¿cuál es el nuevo dominio y rango?

---

### Ejercicio 3

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \sqrt{x+4}
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(-4,0)$ pertenece a la gráfica.
- La expresión representa una función radical.
- El dominio es $[-4, \infty)$.
- El rango es $[0, \infty)$.
- La función es decreciente.
- La gráfica corta al eje $y$ en $y=2$.
- La función es inyectiva.
- La función no está definida para $x < -4$.

#### Sección 5 — Nivel estructural avanzado
- Si se define $y = -\sqrt{x+4}$, ¿cómo cambia la gráfica? ¿Sigue siendo función?
- Explique por qué el rango de $y = \sqrt{x+4}$ es $[0, \infty)$ y no incluye números negativos.

---

### Ejercicio 4

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = 2^x
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,1)$ pertenece a la gráfica.
- La función es exponencial.
- El dominio es $\mathbb{R}$.
- El rango es $(0, \infty)$.
- La función es creciente.
- La gráfica corta al eje $x$ en algún punto.
- La función es inyectiva.
- Para $x=2$, $y=4$.

#### Sección 5 — Nivel estructural avanzado
- Compare $y=2^x$ con $y=3^x$. ¿Qué diferencias hay en el crecimiento?
- Si se define $y = 2^{-x}$, ¿cómo se transforma la gráfica? ¿Es creciente o decreciente?

---

### Ejercicio 5

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
x^2 + y^2 = 9
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una relación $R \subseteq \mathbb{R} \times \mathbb{R}$.
11. Si fuera función, escriba la notación formal; si no, explique por qué no puede definirse como función.
12. Determine si la relación es total respecto a $\mathbb{R}$ (en el sentido de que para cada $x$ exista al menos un $y$).
13. Determine si la relación es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(3,0)$ pertenece a la gráfica.
- La expresión representa una circunferencia.
- El dominio es $[-3,3]$.
- El rango es $[-3,3]$.
- La gráfica es simétrica respecto al origen.
- Para $x=2$, existen dos valores de $y$.
- La relación es una función.
- El punto $(0,4)$ no pertenece a la gráfica.

#### Sección 5 — Nivel estructural avanzado
- Explique por qué esta relación no es una función. ¿Cómo podría restringirse el dominio para que sea función?
- Compare con $x^2 + y^2 = 0$. ¿Qué representa?

---

### Ejercicio 6

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = -x^2 + 2x + 1
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente (vértice).
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,1)$ pertenece a la gráfica.
- El vértice es $(1,2)$.
- El dominio es $\mathbb{R}$.
- El rango es $(-\infty, 2]$.
- La función es creciente en $(-\infty, 1)$.
- La gráfica corta al eje $x$ en dos puntos.
- La función es par.
- El discriminante es positivo.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = -x^2 + 2x + 2$. ¿Cómo cambia el vértice?
- Si se define $y = -x^2 + 2x + 1$ para $x \in [0,2]$, ¿cuál es el nuevo rango?

---

### Ejercicio 7

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \sqrt[3]{x}
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(8,2)$ pertenece a la gráfica.
- La función es impar.
- El dominio es $\mathbb{R}$.
- El rango es $\mathbb{R}$.
- La función es creciente.
- La gráfica corta al eje $x$ en $x=0$.
- La función es inyectiva.
- Para $x=-8$, $y=-2$.

#### Sección 5 — Nivel estructural avanzado
- Compare $y=\sqrt[3]{x}$ con $y=\sqrt{x}$. ¿En qué se diferencian dominio y rango?
- Si se define $y = \sqrt[3]{x} + 1$, ¿cómo se desplaza la gráfica?

---

### Ejercicio 8

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y^2 = x
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una relación $R \subseteq \mathbb{R} \times \mathbb{R}$.
11. Explique por qué no puede definirse como función $f: \mathbb{R} \to \mathbb{R}$.
12. Determine si la relación es total respecto a $\mathbb{R}$ (para cada $x$ existe $y$).
13. Determine si la relación es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(4,2)$ pertenece a la gráfica.
- La expresión representa una parábola horizontal.
- El dominio es $[0, \infty)$.
- El rango es $\mathbb{R}$.
- La gráfica es simétrica respecto al eje $x$.
- Para $x=9$, $y=\pm 3$.
- La relación es una función.
- El punto $(1, -1)$ pertenece a la gráfica.

#### Sección 5 — Nivel estructural avanzado
- Explique por qué la ecuación $y^2 = x$ no define una función. ¿Cómo podría reescribirse para obtener dos funciones?
- Compare con $x^2 = y$. ¿Qué diferencias geométricas hay?

---

### Ejercicio 9

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = e^x
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,1)$ pertenece a la gráfica.
- La función es exponencial natural.
- El dominio es $\mathbb{R}$.
- El rango es $(0, \infty)$.
- La función es creciente.
- La gráfica corta al eje $x$ en algún punto.
- La función es inyectiva.
- Para $x=1$, $y \approx 2.718$.

#### Sección 5 — Nivel estructural avanzado
- Compare $y=e^x$ con $y=2^x$. ¿Cuál crece más rápido para $x>0$?
- Si se define $y = e^{-x}$, ¿cómo cambia la monotonía?

---

### Ejercicio 10

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = -2x^2 + 8x - 5
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente (vértice).
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(2,3)$ pertenece a la gráfica.
- El vértice es $(2,3)$.
- El dominio es $\mathbb{R}$.
- El rango es $(-\infty, 3]$.
- La función es creciente en $(-\infty, 2)$.
- La gráfica corta al eje $x$ en dos puntos.
- La función es par.
- El discriminante es positivo.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = -2x^2 + 8x - 3$. ¿Cómo cambia el vértice?
- Si se restringe el dominio a $[0,4]$, ¿cuál es el rango?

---

### Ejercicio 11

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \frac{1}{2}x + 4
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,4)$ pertenece a la gráfica.
- La pendiente es $\frac{1}{2}$.
- El dominio es $\mathbb{R}$.
- El rango es $\mathbb{R}$.
- La función es creciente.
- La gráfica corta al eje $x$ en $x=-8$.
- La función es inyectiva.
- La ordenada al origen es $4$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = \frac{1}{2}x$. ¿Qué desplazamiento ocurre?
- Si se define $y = \frac{1}{2}x + 4$ para $x \in [-2,2]$, ¿cuál es el rango?

---

### Ejercicio 12

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
|x| + |y| = 1
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una relación $R \subseteq \mathbb{R} \times \mathbb{R}$.
11. Explique por qué no es una función.
12. Determine si la relación es total respecto a $\mathbb{R}$ (para cada $x$ existe $y$).
13. Determine si la relación es inyectiva.

#### Sección 4 — Verdadero o Falso
- El punto $(0,1)$ pertenece a la gráfica.
- La gráfica es un rombo.
- El dominio es $[-1,1]$.
- El rango es $[-1,1]$.
- La gráfica es simétrica respecto a ambos ejes.
- Para $x=0.5$, $y=\pm 0.5$.
- La relación es una función.
- El punto $(0.2, 0.8)$ pertenece a la gráfica.

#### Sección 5 — Nivel estructural avanzado
- Explique por qué esta relación no es función. ¿En qué intervalos de $x$ se obtienen dos valores de $y$?
- Compare con $|x| + |y| = 0$. ¿Qué representa?

---

### Ejercicio 13

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = x^2
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(2,4)$ pertenece a la gráfica.
- La función es par.
- El dominio es $\mathbb{R}$.
- El rango es $[0, \infty)$.
- La función es decreciente en $(-\infty,0)$.
- La gráfica corta al eje $x$ solo en $x=0$.
- La función no es inyectiva.
- El vértice es $(0,0)$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = x^2 + 1$. ¿Cómo se desplaza la gráfica?
- Si se define $y = x^2$ para $x \in [-1,2]$, ¿cuál es el rango?

---

### Ejercicio 14

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
x = y^2
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una relación $R \subseteq \mathbb{R} \times \mathbb{R}$.
11. Explique por qué no es una función.
12. Determine si la relación es total respecto a $\mathbb{R}$ (para cada $x$ existe $y$).
13. Determine si la relación es inyectiva.

#### Sección 4 — Verdadero o Falso
- El punto $(4,2)$ pertenece a la gráfica.
- La expresión representa una parábola horizontal.
- El dominio es $[0, \infty)$.
- El rango es $\mathbb{R}$.
- La gráfica es simétrica respecto al eje $x$.
- Para $x=1$, $y=\pm 1$.
- La relación es una función.
- El punto $(0,0)$ pertenece a la gráfica.

#### Sección 5 — Nivel estructural avanzado
- Explique la diferencia entre $x=y^2$ y $y=x^2$ en términos de función.
- ¿Cómo podría expresarse esta relación como dos funciones?

---

### Ejercicio 15

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \left(\frac{1}{2}\right)^x
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,1)$ pertenece a la gráfica.
- La función es exponencial decreciente.
- El dominio es $\mathbb{R}$.
- El rango es $(0, \infty)$.
- La función es decreciente.
- La gráfica corta al eje $x$ en algún punto.
- La función es inyectiva.
- Para $x=1$, $y=0.5$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = 2^x$. ¿Qué relación hay entre sus gráficas?
- Si se define $y = \left(\frac{1}{2}\right)^{-x}$, ¿cómo se transforma?

---

### Ejercicio 16

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \sqrt{9 - x^2}
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(3,0)$ pertenece a la gráfica.
- La expresión representa una semicircunferencia superior.
- El dominio es $[-3,3]$.
- El rango es $[0,3]$.
- La función es par.
- La función es creciente en $(-3,0)$.
- La función es inyectiva.
- La gráfica corta al eje $y$ en $y=3$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = -\sqrt{9-x^2}$. ¿Qué obtiene?
- Si se define $y = \sqrt{9-x^2}$ para $x \in [0,3]$, ¿cuál es el rango? ¿Es inyectiva en ese intervalo?

---

### Ejercicio 17

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = 5
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,5)$ pertenece a la gráfica.
- La función es constante.
- El dominio es $\mathbb{R}$.
- El rango es $\{5\}$.
- La función no es inyectiva.
- La gráfica es una recta horizontal.
- La función es par.
- La gráfica corta al eje $x$ en algún punto.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = c$ para diferentes valores de $c$. ¿Qué representan?
- Si se define $y = 5$ para $x \in [0,10]$, ¿sigue siendo función? ¿Cuál es su rango?

---

### Ejercicio 18

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
(x-1)^2 + (y+2)^2 = 4
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una relación $R \subseteq \mathbb{R} \times \mathbb{R}$.
11. Explique por qué no es una función.
12. Determine si la relación es total respecto a $\mathbb{R}$ (para cada $x$ existe $y$).
13. Determine si la relación es inyectiva.

#### Sección 4 — Verdadero o Falso
- El punto $(1,-2)$ es el centro de la circunferencia.
- El dominio es $[-1,3]$.
- El rango es $[-4,0]$.
- La gráfica es simétrica respecto al centro.
- Para $x=1$, $y=-2$ y $y=-2$ (solo un punto).
- La relación es una función.
- El punto $(3,-2)$ pertenece a la gráfica.
- La relación no es función porque para algunos $x$ hay dos $y$.

#### Sección 5 — Nivel estructural avanzado
- Si se despeja $y$, se obtienen dos expresiones. Escríbalas.
- ¿Cómo se modifica la gráfica si se suma una constante a $x$ o $y$?

---

### Ejercicio 19

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \frac{2}{3}x - 1
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(0,-1)$ pertenece a la gráfica.
- La pendiente es $\frac{2}{3}$.
- El dominio es $\mathbb{R}$.
- El rango es $\mathbb{R}$.
- La función es creciente.
- La gráfica corta al eje $x$ en $x=1.5$.
- La función es inyectiva.
- La ordenada al origen es $-1$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = \frac{2}{3}x$. ¿Qué desplazamiento ocurre?
- Si se define $y = \frac{2}{3}x - 1$ para $x \in [-3,3]$, ¿cuál es el rango?

---

### Ejercicio 20

En el campo de los números reales y el plano cartesiano $\mathbb{R} \times \mathbb{R}$.

$$
y = \sqrt{x-2} + 1
$$

#### Sección 1 — Construcción y análisis geométrico
1. Determine los puntos de corte con los ejes.
2. Describa el comportamiento general de la gráfica.
3. Indique si existe simetría respecto a algún eje.
4. Determine si es creciente o decreciente (si aplica).
5. Determine si representa una función usando la prueba de la recta vertical.

#### Sección 2 — Dominio y rango
6. Determine el dominio justificando cada restricción.
7. Determine el rango justificando geométricamente.
8. Escriba dominio y rango en notación de intervalos.
9. Indique si el dominio es abierto, cerrado o mixto.

#### Sección 3 — Definición formal
10. Defina por comprensión una función $F \subseteq \mathbb{R} \times \mathbb{R}$.
11. Escriba la función en notación formal $f: A \to B$.
12. Determine si la función es total respecto a $\mathbb{R}$.
13. Determine si es inyectiva (si aplica).

#### Sección 4 — Verdadero o Falso
- El punto $(2,1)$ pertenece a la gráfica.
- El dominio es $[2, \infty)$.
- El rango es $[1, \infty)$.
- La función es creciente.
- La gráfica corta al eje $y$ en algún punto.
- La función es inyectiva.
- Para $x=3$, $y=2$.
- La función es una traslación de $y=\sqrt{x}$.

#### Sección 5 — Nivel estructural avanzado
- Compare con $y = \sqrt{x} + 1$. ¿En qué se diferencian?
- Si se define $y = \sqrt{x-2} + 1$ para $x \in [2,6]$, ¿cuál es el rango?

---

Fin del cuestionario.